# ========================================
# SERVER
# ========================================
server:
  port: ${PORT:8080}

# ========================================
# SPRING CONFIG
# ========================================
spring:
  application:
    name: freelancing-platform

  devtools:
    restart:
      enabled: false

  # DATABASE (MySQL)
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000

  # JPA / HIBERNATE
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

  # EMAIL (Gmail SMTP)
  mail:
    host: smtp.gmail.com
    port: 587
    username: your_email@gmail.com
    password: your_app_password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  # FILE UPLOAD
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

# ========================================
# JWT
# ========================================
app:
  jwt:
    secret: FreelancingPlatformSuperSecretKeyThatIsAtLeast256BitsLongForHS256Algorithm2026
    access-token-expiration: 86400000
    refresh-token-expiration: 604800000

  # FILE STORAGE
  file:
    upload-dir: ./uploads

  # CORS
  cors:
    allowed-origins:${CORS_ALLOWED_ORIGINS:https://freelancing-pearl.vercel.app}


# ========================================
# SWAGGER / OPENAPI
# ========================================
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  default-produces-media-type: application/json

# ========================================
# LOGGING
# ========================================
logging:
  level:
    root: INFO
    com.freelancing: DEBUG
    org.hibernate.SQL: WARN
